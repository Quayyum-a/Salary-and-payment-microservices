# THIS FILE HAS BEEN REMOVED - use .env instead
# For safety the example file is replaced with this message.
# The active environment file is `.env` at the repository root.
#
# If you still want an example template, copy `.env` and redact secrets.